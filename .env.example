#-----------------------------------------------------------------------------
# Storage Snapshot Environment Configuration
#
# Purpose: Defines environment variables for S3 backup and restore operations
# Context: Used by container startup and backup/restore scripts
# Note: Copy this file to .env and modify values according to your setup
#-----------------------------------------------------------------------------



#-----------------------------------------------------------------------------
# REQUIRED S3 STORAGE CONFIGURATION
#-----------------------------------------------------------------------------
# S3 bucket name for storing backup files
S3_BUCKET=your-backup-bucket-name

# S3 prefix/path within the bucket for organizing backups
S3_PREFIX=backups

# AWS region where the S3 bucket is located
S3_REGION=us-east-1

#-----------------------------------------------------------------------------
# BACKUP CONFIGURATION
#-----------------------------------------------------------------------------
# Host directory path for volume mounting (outside container)
HOST_BACKUP_PATH=/home/backups

# Backup directory inside container
CONTAINER_BACKUP_PATH=/home/backups

# Base name for backup files (timestamp will be appended)
BACKUP_FILE_NAME=storage-snapshot

# Number of days to keep old backups (optional, leave empty for no cleanup)
BACKUP_KEEP_DAYS=30

#-----------------------------------------------------------------------------
# ENCRYPTION CONFIGURATION
#-----------------------------------------------------------------------------
# GPG passphrase for encrypting/decrypting backups (optional)
# Leave empty to store backups without encryption
GPG_PASSPHRASE=your-gpg-passphrase

#-----------------------------------------------------------------------------
# AWS AUTHENTICATION
#-----------------------------------------------------------------------------
# AWS access key ID for S3 authentication (optional if using IAM roles)
S3_ACCESS_KEY_ID=your-access-key-id

# AWS secret access key for S3 authentication (optional if using IAM roles)
S3_SECRET_ACCESS_KEY=your-secret-access-key

#-----------------------------------------------------------------------------
# S3 COMPATIBLE STORAGE
#-----------------------------------------------------------------------------
# Custom S3 endpoint URL (optional, for S3-compatible services like MinIO)
S3_ENDPOINT=

# Enable S3v4 signature version (set to "yes" for some S3-compatible services)
S3_S3V4=no

#-----------------------------------------------------------------------------
# SCHEDULING CONFIGURATION
#-----------------------------------------------------------------------------
# Cron schedule for automatic backups (optional, leave empty for manual execution)
# Format: "0 2 * * *" for daily at 2 AM
CRON_SCHEDULE=
